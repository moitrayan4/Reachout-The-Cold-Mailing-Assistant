# Core AI/LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-groq>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0

# MCP
mcp[cli]>=1.0.0

# Gmail API (OAuth) — draft creation when IMAP/SMTP basic-auth is blocked
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0

# Database
sqlmodel>=0.0.21
sqlalchemy>=2.0.0

# Config / Settings
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Resume parsing
pdfplumber>=0.10.0
python-docx>=1.0.0

# File watching
watchdog>=4.0.0

# Scheduling
apscheduler>=3.10.0

# Web scraping / HTML parsing
playwright>=1.40.0
patchright>=1.40.0          # patched, undetected Playwright (Akamai/Cloudflare)
curl_cffi>=0.7.0            # browser-TLS-impersonating HTTP client
selectolax>=0.3.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
httpx>=0.27.0
aiohttp>=3.9.0

# NLP / Embeddings (for MatchScorer)
sentence-transformers>=3.0.0

# CLI / UI
rich>=13.0.0
click>=8.0.0
streamlit>=1.33.0          # dynamic web control-panel UI (app.py)
pandas>=2.0.0              # tables in the Streamlit UI

# Utilities
python-dateutil>=2.9.0
pytz>=2024.1
